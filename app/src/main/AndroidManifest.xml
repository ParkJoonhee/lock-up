<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />



    <application
        android:allowBackup="true"
        android:icon="@drawable/doorlock"
        android:label="@string/app_name"
        android:roundIcon="@drawable/doorlock"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".JoinActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".HNoticeActivity" />
        <activity android:name=".HgNoticeActivity" />
        <activity android:name=".GNoticeActivity" />
        <activity android:name=".GgNoticeActivity" />
        <activity android:name=".NoticeaddActivity" />
    </application>

</manifest>